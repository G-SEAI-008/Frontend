# Vendored Anti-Slop

Source: https://github.com/dmmulroy/anti-slop

Copied without modifications with the install-anti-slop skill installer from
`/home/kawa/.agents/skills/install-anti-slop/assets/anti-slop`.
Installed path: `tools/oxlint/anti-slop/`.
Exact upstream commit: unknown; the bundled assets contain no root provenance record.
The pristine copied files are retained here and their SHA-256 hashes are recorded
in `migration-checks/anti-slop-snapshot.sha256`. Nested Stylistic provenance and
license files are preserved. This root provenance file is the only addition.
Only the generic plugin is enabled; Effect is not a direct dependency.
