import 'server-only';

// TODO 1: Erstelle getPosts(), getPost(id), getPokemonList() und getPokemon(id).
// Jede Funktion: fetch → response.ok prüfen → response.json() → Schema.parse(data).
// Die URLs stehen in der README. Die Schemas findest du in schemas.ts.
// Verwende bei fetch die Option { cache: 'no-store' }.
// Bei einem Detail-Aufruf mit Status 404: notFound() aus next/navigation aufrufen.
