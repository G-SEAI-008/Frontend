// TODO 3: Ergänze 'use client', eine promise-Prop und use(promise).
// Den passenden Typ findest du in ./_lib/schemas.ts.
const PokemonList = () => {
  return <p>Hier werden später die geladenen Daten angezeigt.</p>;
};

export default PokemonList;
